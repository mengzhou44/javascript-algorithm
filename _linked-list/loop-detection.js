const {LinkedList, Node } = require("./_linked-list")
 

function isCircular(l) {
     
    
}
 
let node = new Node("c",new Node("t", new Node()))

let head = new Node("a", new Node())
 
console.log(isInterection(l1,l2))